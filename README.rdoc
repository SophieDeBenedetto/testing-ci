## to do:

- fix status message--why is "default" still showing up? why is "context" all wonky?
- presenter object for error_messages object on result show page
- general styling for that page

## DEPLOY!!